<UserControl x:Class="Carrezance.Support.App.Views.NetworkView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock Style="{StaticResource PageTitle}" Text="Internet &amp; Réseau"/>
        <TextBlock Text="Contrôlez la connexion et appliquez une réparation simple de l'accès aux sites web."
                   Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,18"/>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Tester la connexion Internet" Command="{Binding TestInternetCommand}"/>
            <Button Content="Réparer l'accès aux sites web" Command="{Binding FlushDnsCommand}" Style="{StaticResource SecondaryButton}"/>
            <Button Content="Afficher la configuration réseau" Command="{Binding ShowConfigurationCommand}" Style="{StaticResource SecondaryButton}"/>
        </StackPanel>

        <Border Style="{StaticResource Card}" Padding="12" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding LastActionBadge}" FontWeight="SemiBold" Margin="0,0,14,0"/>
                <TextBlock Text="{Binding ActionStatus}" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Border Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock Style="{StaticResource SectionTitle}" Text="Résultats des tests"/>
                <ItemsControl ItemsSource="{Binding Results}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="190"/>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="1" Text="{Binding Status}"/>
                                <TextBlock Grid.Column="2" Text="{Binding Message}" TextWrapping="Wrap"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <Border Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock Style="{StaticResource SectionTitle}" Text="Configuration réseau"/>
                <TextBox Text="{Binding Configuration, Mode=OneWay}" IsReadOnly="True" MinHeight="120" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
